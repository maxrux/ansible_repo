- name: Aggiungi utente pippo
  hosts: all
  become: true

  tasks:
    - name: Crea utente pippo
      ansible.builtin.user:
        name: pippo
        password: "{{ 'pippo01' | password_hash('sha512') }}"
        state: present
        shell: /bin/bash
        create_home: true
